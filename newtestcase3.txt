$$
     integer low, high, step;
     boolean baddie;
$$
     scan(low, high, step);
     baddie = true;   (* example boolean use *)
     while (low <= high)
     {
         print(low);
         low = low + step;
     }
     endwhile
$$