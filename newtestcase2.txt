$$
integer a, b, c, d, e, f, result;
$$
a = 10;
b = 5;
c = 2;
d = 8;
e = 3;
f = 4;

result = a + b * c;
[*Should use PUSHM, PUSHM, PUSHM, M, A, POPM*]
result = d - e * f; 
[*Should use PUSHM, PUSHM, PUSHM, M, S, POPM*]
result = (a + b) * (c + d);
[*Should use PUSHM, PUSHM, A, PUSHM, PUSHM, A, M, POPM*]
result = a * b / c;
[*Should use PUSHM, PUSHM, M, PUSHM, D, POPM*]
$$